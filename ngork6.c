#include <stdio.h>
	int main(){
		int principal= 636, years=90 ,rate=90;
		int simpleintrest = (principal*rate*years)/100;
		printf("the value of simpleintrest is %d", simpleintrest);
	return 0;
}
