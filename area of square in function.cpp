#include <stdio.h>
#include <math.h>

int main(){
     int side;
	 printf("the value of side is\n",side);
	 scanf("%d", &side);
	 printf("the value of area is %f", pow(side,2));	
	return 0;
}
